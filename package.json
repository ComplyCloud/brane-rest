{
  "name": "@complycloud/brane-rest",
  "version": "0.0.0",
  "description": "REST interface for brane services",
  "main": "src",
  "repository": "https://github.com/complycloud/brane-graphql",
  "author": "Sean Bruton <sean@sean.cloud>",
  "license": "MIT",
  "scripts": {
    "check": "yarn lint && yarn test",
    "lint": "eslint src test",
    "test": "mocha ${WATCH:+--watch} ${DEBUGGER:+--inspect --debug-brk} --recursive --compilers js:babel-register",
    "test:watch": "WATCH=true yarn test"
  },
  "devDependencies": {
    "babel-eslint": "^8.0.0",
    "babel-preset-env": "^1.6.0",
    "babel-register": "^6.26.0",
    "chai": "^4.1.1",
    "eslint": "^4.5.0",
    "eslint-config-airbnb-base": "^12.0.0",
    "eslint-plugin-import": "^2.7.0",
    "mocha": "^3.5.0",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": [
    "check"
  ],
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": true
          }
        }
      ]
    ]
  }
}
